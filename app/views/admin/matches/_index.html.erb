<table id="matches-table" class="table table-striped table-bordered" width="100%">
  <thead>
    <tr>
      <th>Number</th>
      <th>Date</th>
      <th>Phase</th>
      <th>Team 1</th>
      <th>Team 1 Score</th>
      <th>Team 2 Score</th>
      <th>Team 2</th>
      <th>Show?</th>
      <th>Ended?</th>
      <th>Actions</th>
    </tr>
  </thead>
  <% matches.each do |match| %>
    <tr>
      <td><%= match.number %></td>
      <td><%= I18n.l match.date, format: :short %></td>
      <td><%= match.phase.name %></td>
      <td><%= match.team1.name if match.team1 %></td>
      <td><%= match.team1_score %></td>
      <td><%= match.team2_score %></td>
      <td><%= match.team2.name if match.team2 %></td>
      <td><%= yes_no_label(match.show) %></td>
      <td><%= yes_no_label(match.ended) %></td>
      <td>
        <%= link_to edit_admin_match_path(match), remote: true,
          class: 'btn btn-default btn-sm' do %>
          <span class="glyphicon glyphicon-pencil"></span>
        <% end %>
      </td>
    </tr>
  <% end %>
</table>

<script type="text/javascript">
  $(document).ready(function() {
     $('#matches-table').DataTable({
       retrieve: true,
       iDisplayLength: 64,
       paging: false,
       info: false,
       columns: [
         { type: "num" },
         { type: "string" },
         { type: "string" },
         { type: "string" },
         { orderable: false },
         { orderable: false },
         { type: "string" },
         { type: "string" },
         { type: "string" },
         { orderable: false }
       ]
    });
  });
</script>
